server:
  port: 8761

spring:
  profiles:
    active: euraka_client,logback,euraka_server
  application:
      name: discovery-service
  cloud:
    config:
      name: common-config
      uri: http://localhost:19300/config

eureka:
  uri: http://localhost:8761/eureka/
