version = '1.0'
jar {
    baseName = 'webapp-manager-gateway' + "-$profile"
    version = '0.1.0'
}

dependencies {
    compile(
            'org.springframework.boot:spring-boot-starter-data-rest'
    )
}