server:
  port: 19305
spring:
  profiles:
    active: eureka_client,common
  application:
      name: mqtt-client
  cloud:
    config:
      name: common-config
      uri: http://localhost:19300/config

service:
  eureka:
    uri: http://localhost:8761/eureka/


#  jwt  token验证方式的配置, 需添加 "org.springframework.security:spring-security-jwt" 包
#security:
#  oauth2:
#    client:
#      client-id: webapp
#      client-secret: webapp
#    resource:
#      jwt:
#        key-uri: http://localhost:19301/security/oauth/token_key