spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JerryHanmin/vic-tech-config.git
#          username: JerryHanmin
#          password: {password}
          searchPaths: applications,i18n,avros
