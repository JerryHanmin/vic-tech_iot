version = '1.0'
jar {
    baseName = 'config-service'+"-$profile"
    version = '0.1.0'
}

dependencies {
    compile(
            "org.springframework.cloud:spring-cloud-config-server",
    )
    compile project(
            ':iot-components:iot-banner'
    )
}