spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JerryHanmin/vic-tech-config.git
          username: JerryHanmin
          password: ${password}
          searchPaths: applications

logback:
  custom_level: debug
  root_level: info
  path: logs
  custom_path: com.vic