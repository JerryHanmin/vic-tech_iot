version = '1.0'
jar {
    baseName = 'device-service' + "-$profile"
    version = '0.1.0'
}

dependencies {
    compile(
            "org.springframework.boot:spring-boot-starter-data-mongodb"
    )
}